<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.imyasfinal">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Booking"></activity>
        <activity android:name=".ArtistProfile" />
        <activity android:name=".AddImages" />
        <activity android:name=".ArtistAddList" />
        <activity android:name=".PendingStatus" />
        <activity android:name=".ArtistDetail" />
        <activity android:name=".ArtistList" />
        <activity
            android:name=".ArtistHome"
            android:label="@string/title_activity_artist_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SplashScreen" />
        <activity
            android:name=".Home"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".LoginArtist" />
        <activity android:name=".RegisterArtist" />
        <activity android:name=".Register" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>